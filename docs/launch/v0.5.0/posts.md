# ccmux v0.5.0 — X drafts

Drafts only. Publish after the v0.5.0 release and website deployment are verified.
Current media are offline Muse echo previews pending authenticated acceptance.
Replace them with `CCMUX_MUSE_PROVIDER=meta` recordings before using these drafts.
No posts have been sent to X.

## 1. Release announcement

ccmux v0.5.0 adds Meta Muse Code. Start a project, detach, and come back to the same session—from your terminal or phone. Native Muse history and token usage are built in.

Get started: https://ccmux.ai/docs/muse-code/

Attach: `muse-start.mp4` (GIF alternative: `muse-start.gif`).
Alt text: Starting a Muse Code project with ccmux, receiving a response, and detaching to the ccmux session dashboard.

## 2. Resume without hunting for IDs

Your Muse conversations now live alongside your other coding agents in ccmux. Search the history, preview a thread, press Enter, and resume that exact session.

https://ccmux.ai/docs/muse-code/

Attach: `muse-resume.mp4` (GIF alternative: `muse-resume.gif`).
Alt text: The ccmux Conversations screen previews a Muse thread, then opens that same conversation in the native Muse CLI.

## 3. Contribution invitation

Use Muse Code? Help us make ccmux better. Try v0.5.0 and send feedback—or a PR with a redacted log fixture. Docs are available in nine languages, including Russian.

https://github.com/skzv/ccmux

Attach: `muse-history.png`, `muse-session.png`, `muse-dashboard.png`.
Alt text, in order: Muse conversation history and preview in ccmux; a native Muse response; the ccmux dashboard showing a Muse session waiting for input.

## Capture notes

- Two VHS tapes under `docs/vhs/`; each targets 15–30 seconds, 1600×840.
- MP4 is the primary X upload; GIF is a fallback. PNGs are standalone stills.
- Disposable content only; native authentication is never filmed.
- Watch each clip and inspect the screenshots after a rerender. Model response
  latency can require adjusting the tape's sleep before capture.
